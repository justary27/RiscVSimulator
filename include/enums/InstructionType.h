#ifndef INSTRUCTION_TYPE_H
#define INSTRUCTION_TYPE_H

/// @brief The instruction types of RISC-V
/// architecture available in this
/// simulator.
enum InstructionType {
    rType,
    iType,
    bType,
    jType,
    sType,
};

#endif
